<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.diary.mapper.LikeMapper">

    <insert id="insertLike">
        insert into diary_likes (diary_id, user_id) values (#{diaryId}, #{userId})
    </insert>

    <delete id="deleteLike">
        delete from diary_likes where diary_id = #{diaryId} and user_id = #{userId}
    </delete>

    <select id="isLiked" resultType="boolean">
        select count(*) > 0 from diary_likes where diary_id = #{diaryId} and user_id = #{userId}
    </select>

    <select id="countLikes" resultType="int">
        select count(*) from diary_likes where diary_id = #{diaryId}
    </select>
</mapper>